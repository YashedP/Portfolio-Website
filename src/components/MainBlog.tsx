import { Card, CardTitle, CardContent } from "@/components/ui/card";
import { motion } from "framer-motion";

export default function SlidingCard({ articles }: { articles: string[] }) {
    return (
        <>
            <div>
                Testing
            </div>
        </>
    );
};
